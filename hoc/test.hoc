/*
include "str.hoc";
include "math.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
include "sort.hoc";
*/

/*
var int a;
var int b;

var [1]int* t;
var char* c = "abc";
*/

/*
proc int foo(char s)
{
  return 0;
}
*/

proc int bar(int x, float v)
{
  var char* p;
  return 0;
}

proc void main(int za)
{
  var int a;
  var int b;
  var int c;
  var float v;
  a = (a - bar(b, v)) * bar(c, (type float)1);
}


