include "hlib\\math.hoc";
include "hlib\\str.hoc";
/*
include "hlib\\sort.hoc";
include "hlib\\fibo.hoc";
include "hlib\\fizzbuzz.hoc";
include "hlib\\power.hoc";
include "hlib\\gcd.hoc";
*/

/*
proc void print_numbers(int* numbers, int len)
{
  var int i = 0;
  while(i < len)
  {
    print_int(numbers[i]);
    print_str("\n");

    i = i+1;
  }
}

proc int* make_numbers(int how_many)
{
  var int i = 0;
  var int c1 = 17;
  var int c2 = 31;
  var int* numbers = new(int, how_many);

  while(i < how_many)
  {
    if((i % 2) == 0)
      numbers[i] = c1 * i;
    else
      numbers[i] = c2 * i;

    i = i+1;
  }
  return numbers;
}
*/

proc void print_fract(float f)
{
  var int k = 0;
  var float R = f;
  var float M = ipow(2.0, -1) / 2;
}

proc int main()
{
  var float x = ipow(2.0, -4);
  print_int(cast(int)x);
  /*
  var int y = int_log2(31);
  print_int(y);
  */

  /*
  fibo(40);
  print_str("-----------\n");
  fizzbuzz(50);
  */
  return 0;
}

