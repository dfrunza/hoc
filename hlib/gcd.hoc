proc int gcd_(var int a, var int b)
{
  var int result = a;
  if(b != 0)
  {
    result = b;
    var int r = a % b;
    if(r == 0)
      result = b;
    else
      result = gcd_(b, r);
  }
  return result;
}

/*
   Greatest Common Divisor
 */
proc int gcd(var int a, var int b)
{
  return gcd_(abs(a), abs(b));
}

/*
   Least Common Multiple
 */
proc int lcm(var int a, var int b)
{
  return abs(a*b)/gcd(a, b);
}


