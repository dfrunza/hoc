/*
include "str.hoc";
include "math.hoc";
include "sort.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
*/

proc float foo(int a)
{
  return (type float)a;
}

proc void main()
{
  var int i = bar();

  proc int bar()
  {
    return 11;
  }
}

main();

