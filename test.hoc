include "hlib\\str.hoc";
/*
include "hlib\\fibo.hoc";
include "hlib\\fizzbuzz.hoc";
include "hlib\\abs.hoc";
include "hlib\\sqrt.hoc";
include "hlib\\power.hoc";
include "hlib\\minmax.hoc";
include "hlib\\gcd.hoc";
*/

/*
proc void print_numbers(int* numbers, int len)
{
  var int i = 0;
  while(i < len)
  {
    print_int(i);
    print_str("=");
    print_int(*(numbers+i));
    print_str("\n");

    i = i+1;
  }
}

proc int* make_numbers(int how_many)
{
  var int i = 0;
  var int constant = 17;
  var int* numbers = new(int, how_many);

  while(i < how_many)
  {
    *(numbers+i) = constant*i;

    i = i+1;
  }
  return numbers;
}
*/

proc [4]char make_array()
{
  return "ABC";
}

proc int main()
{
  var [4]char a = make_array();
  print_str(a);

  /*
  var int count = 10;
  var int* numbers = make_numbers(count);
  print_numbers(numbers, count);
  */

  /*
  print_str("Hi there!\n");
  fibo(40);
  print_str("-----------\n");
  fizzbuzz(200);
  */
  return 0;
}

