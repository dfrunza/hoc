/*
include "str.hoc";
include "math.hoc";
include "sort.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
*/

proc float foo(int a)
{
  return (type float)a;
}

proc void main()
{
  foo(123);
}

main();

