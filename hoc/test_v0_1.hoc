include "math.hoc";
include "str.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
include "sort.hoc";
include "std.hoc";
/*
*/

void print_numbers([]int numbers, int len)
{
  int i = 0;
  while(i < len)
  {
    print_int(numbers[i]);
    print_str(&"\n");

    i = i+1;
  }
}

[]int make_numbers(int how_many)
{
  int i = 0;
  int c1 = 17;
  int c2 = 31;
  []int numbers = ([]int):alloc(how_many);

  while(i < how_many)
  {
    if((i mod 2) == 0)
      numbers[i] = c1 * i;
    else
      numbers[i] = c2 * i;

    i = i+1;
  }
  return numbers;
}

void main()
{
  float x = -12.0;
  float y = 1.058;
  float z = x + y;
  print_str(&"\n");
  print_float(x);
  print_str(&" + ");
  print_float(y);
  print_str(&" = ");
  print_float(z);
  print_str(&"\n");

  int a = 7*7*5*3;
  int b = 7*3*11;
  int r = gcd(a, b);
  print_str(&"\ngcd(");
  print_int(a);
  print_str(&",");
  print_int(b);
  print_str(&")=");
  print_int(r);
  print_str(&"\n");

  int nr_count = 15;
  []int numbers = make_numbers(nr_count);
  print_title(&"SHUFFLED");
  print_numbers(numbers, nr_count);

  quick_sort(numbers, nr_count);
  print_title(&"SORTED");
  print_numbers(numbers, nr_count);

  print_title(&"FIBONACCI");
  fibo(20);

  print_title(&"FIZZBUZZ");
  fizzbuzz(30);
}
