/*
include "str.hoc";
include "math.hoc";
include "sort.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
*/

proc char main()
{
  var char* arr = &"abc";
  var char c = *(arr + (type char*)1);
}

main();

