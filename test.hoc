/*
include "hlib/abs.hoc"
include "hlib/minmax.hoc"
include "hlib/gcd.hoc"
include "hlib/power.hoc"
include "hlib/sqrt.hoc"
include "hlib/fizzbuzz.hoc"
*/

proc bool foo(var float a, var int b, var bool y)
{
  return !(b);
}

proc int main()
{
  var float b = 223.3;
  var bool c;
  var bool a = false;
  if(c)
  {
    c = foo(b*b, 99, a);
    c = (a && c) || c;
  }
  return 0;
}

