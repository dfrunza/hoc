proc int max(var int a, var int b)
{
  var int result = a;
  if(a < b)
    result = b;
  return result;
}

proc int min(var int a, var int b)
{
  var int result = a;
  if(a > b)
    result = b;
  return result;
}

