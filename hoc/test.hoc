include "str.hoc";
include "math.hoc";
/*
include "fibo.hoc";
include "fizzbuzz.hoc";
include "sort.hoc";
*/

var int a = 112;
var [1]int* t;
var int b = a;
var char* c = "abc";

proc int foo(int bubbles, bool troubles)
{
  var float nonsense = main();
  return nonsense;
}
/*
*/

proc int main()
{
  a = (type int*)(b);
  foo(a, b);
  if(true)
    return 0;
  else
    a = b;

  {
    var float x = -1.;
  }

  while(true)
    a = 2;
}

