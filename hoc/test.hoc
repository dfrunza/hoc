/*
include "str.hoc";
include "math.hoc";
include "sort.hoc";
include "fibo.hoc";
include "fizzbuzz.hoc";
*/

proc char main()
{
  var char* p_h = &"abc";
  var char c = *(p_h + (type char*)1);
}

main();

