proc is_equal(var a, var b)
{
  var result;
  var diff = a-b;
  if(diff)
    result = 0;
  else
    result = 1;
  return result;
}

proc main()
{
  var a = -1;
  var b = -1;

  var c = 4;
  if(is_equal(a, b))
    ;
  else if(is_equal(a, b))
    c=c/2;
  else if(1)
    c = 3;
  return c;
}

